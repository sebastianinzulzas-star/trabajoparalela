#pragma once
#include <string>

// Validación de CSV y entradas
bool validarCabecerasEstudiantes(const std::string& linea);
bool validarCabecerasCorrectas(const std::string& linea);
bool validarCabecerasPaes(const std::string& linea);
